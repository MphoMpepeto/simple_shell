#include "shell.h"
/**
 * main - entry point of simple shell
 * Return: 0
 * Description: This is the entry point of a simple shell project
 */

int main(void)
{
/*	char *nipa_input = NULL, *full_path = NULL;
	char *copy_nipa = NULL, *copy_path = NULL;
	char **argv = NULL, **array_of_pokemons = NULL;*/

	while (true)
	{
		kyere_prompt();
		gye_input();
		/*free(nipa_input);
		free(copy_nipa);
		free(array_of_pokemons);
		free(full_path);
		free(copy_path);
		free_argv(argv);*/
	}
	return (0);
}
